<%- include('partials/header', { title: message }) %>

<div class="empty-state-full">
  <div class="empty-state-content">
    <h2><%= message %></h2>
    <p><%= description %></p>
    
    <div class="empty-state-help">
      <h3>Getting Started</h3>
      <p>To generate a changelog, run the following command in your git repository:</p>
      <pre><code>greptile generate --since "2 weeks ago"</code></pre>
      
      <p>Once you've generated a changelog, refresh this page to see it.</p>
      
      <h3>Need Help?</h3>
      <p>Check the README for more information on how to use Greptile.</p>
    </div>
  </div>
</div>

<%- include('partials/footer') %> 